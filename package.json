{
  "name": "piano-chords-lab",
  "version": "0.1.0",
  "description": "Piano Chords Lab — interactive piano chord, scale & theory reference (freemium iOS PWA)",
  "scripts": {
    "build": "mkdir -p www && cp index.html app.js sw.js manifest.json www/ && ([ -d icons ] && cp -r icons www/ || true)",
    "sync": "npm run build && npx cap sync ios",
    "test": "node --test test/*.test.cjs"
  },
  "author": "Pablo Honey",
  "license": "ISC",
  "dependencies": {
    "@capacitor/cli": "^8.4.0",
    "@capacitor/core": "^8.4.0",
    "@capacitor/ios": "^8.4.0",
    "@revenuecat/purchases-capacitor": "^11.0.0"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5",
    "playwright": "^1.48.0",
    "react": "18.2.0",
    "react-dom": "18.2.0"
  }
}
